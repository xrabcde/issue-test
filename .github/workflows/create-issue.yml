name: Create Issue

on:
  workflow_dispatch:

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: nashmaniac/create-issue-action@v1.2
        with:
          title: Build Failed
          token: ${{secrets.TOKEN}}
          labels: bug
